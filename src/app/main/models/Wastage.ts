export default class Wastage {
  id: number;
  book: string;
  Note: string;

  constructor(id: number, book: string, Note: string) {
    this.id = id;
    this.book = book;
    this.Note = Note;
  }
}
