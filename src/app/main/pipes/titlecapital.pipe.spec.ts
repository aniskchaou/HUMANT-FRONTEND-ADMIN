import { TitlecapitalPipe } from './titlecapital.pipe';

describe('TitlecapitalPipe', () => {
  it('create an instance', () => {
    const pipe = new TitlecapitalPipe();
    expect(pipe).toBeTruthy();
  });
});
